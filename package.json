{
  "name": "vesting",
  "version": "1.0.0",
  "description": "**Foundry is a blazing fast, portable and modular toolkit for Ethereum application development written in Rust.**",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "test": "forge clean && forge test",
    "deploy": "bash -c 'source .env && forge clean && forge script DeployVesting --broadcast --fork-url $RPC --verify --etherscan-api-key $ETHERSCAN_API_KEY'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "solhint": "^4.5.4"
  }
}
